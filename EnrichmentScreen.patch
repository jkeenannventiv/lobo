diff --git a/src/screens/EnrichmentScreen.tsx b/src/screens/EnrichmentScreen.tsx
index fb8a778..50001c8 100644
--- a/src/screens/EnrichmentScreen.tsx
+++ b/src/screens/EnrichmentScreen.tsx
@@ -208,6 +208,16 @@ export default function EnrichmentScreen({ navigation }: any) {
           </View>
         )}
 
+        {done && (
+          <View style={styles.storageTip}>
+            <Text style={styles.storageTipText}>
+              💾 Your Timeline data is now safely stored in Lobo. You can delete the
+              downloaded file (timeline.json or location-history.json) from your
+              Downloads folder to free up space — Lobo doesn't need it anymore.
+            </Text>
+          </View>
+        )}
+
         {done && (
           <TouchableOpacity
             style={styles.button}
@@ -307,6 +317,17 @@ const styles = StyleSheet.create({
     color: '#555570',
     fontSize: 16,
   },
+  storageTip: {
+    backgroundColor: '#f0f4f8',
+    borderRadius: 12,
+    padding: 14,
+    marginBottom: 20,
+  },
+  storageTipText: {
+    fontSize: 13,
+    color: '#555570',
+    lineHeight: 20,
+  },
   reviewCard: {
     backgroundColor: '#f0f4f8',
     borderRadius: 16,
